#include <bits/stdc++.h>
using namespace std;
int approximatePI(int n){
    for(int i=1;int d=2;i<=n;i++;d+=2){
        int s+=4/(d*(d+1)*(d+2))*sign; 
        int sign*=-1; mm
    }
}
int main(){
    int n;
    cin>>n;
    cout<<approximatePI(n);

}
